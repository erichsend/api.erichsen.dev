dependencies:
  redis:
    enabled: false

kong:
  admin:
    enabled: true
    type: ClusterIP
    clusterIP: None

  proxy:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: kic.erichsen.dev

  image:
    repository: kong/kong-gateway
    tag: "3.2"

  ingressController:
    enabled: false

  replicaCount: 3
